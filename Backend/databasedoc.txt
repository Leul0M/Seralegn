================================================================================
Seralegn Platform - Database & Workflow Documentation
================================================================================

This document outlines the database design, core workflows, and transaction
logic for the Seralegn Platform, a gig-economy marketplace connecting Homeowners
and Workers.

--------------------------------------------------------------------------------
1. Architectural Overview
--------------------------------------------------------------------------------
To ensure separation of concerns, robust Row Level Security (RLS), and clean
onboarding flows, the database uses three separate user tables referencing
Supabase's `auth.users` rather than a single unified `profiles` table:

  * home_owners: Simple homeowner profiles.
  * workers: Worker verification data (Fayda National ID), monetization settings,
    status flags, and bans.
  * admins: Administrator accounts for moderating the platform.

--------------------------------------------------------------------------------
2. Platform Workflows
--------------------------------------------------------------------------------

A. Homeowner Workflow
---------------------
1. Registration: Authenticates with Supabase Auth -> Inserts profile in `home_owners`.
2. Posting a Job: Inserts a row into `jobs` with `status = 'open'` and `worker_id = NULL`.
3. Claim Notification: Receives an event/notification when a worker claims the job.
4. Completion: Taps "Confirm Completion" once work is done -> Updates `jobs.status = 'completed'`
   and sets `completed_at = now()`.
5. Flagging (Optional): If a worker exhibits misconduct, the homeowner can submit
   a row in `reports` containing the details.

B. Worker Workflow
------------------
1. Registration & Verification:
   * Enters details + Fayda Number (Ethiopian National ID) -> Inserts profile in
     `workers` with `fayda_verified = false`.
   * On registration, a 30-day free trial is activated (`trial_ends_at = now() + INTERVAL '30 days'`).
   * The worker cannot claim jobs until an admin reviews documents and updates `fayda_verified = true`.
2. Subscription Management (Chapa Gateway):
   * When `trial_ends_at` is close to expiring, the worker pays through Chapa.
   * A ledger record is inserted in `subscriptions` with `status = 'pending'`.
   * On Chapa webhook confirmation, `status` changes to `'success'` and `subscription_expires_at`
     is extended by 30 days.
3. Claiming Jobs (Race-Condition Protected):
   * Worker calls the RPC function `claim_job_securely`.
   * If successful, the job status changes to `'claimed'` with the worker's ID and `claimed_at = now()`.
   * Ghosting Timeout: The worker must start the job. If they do not start it within 90 minutes,
     a cron script runs to release it.
4. Job Execution:
   * Workers tap "Start Job" -> Updates `jobs.status = 'in_progress'` and sets `started_at = now()`.
   * Worker taps "Complete Job" -> Updates `jobs.status = 'pending_confirmation'`.
   * Homeowner confirmation updates the status to `'completed'`.

C. Admin Dashboard Workflow
---------------------------
1. Fayda Verification: Reviews profiles with `fayda_verified = false`, matches documents,
   and sets the flag to `true`.
2. Moderation & Flags:
   * Reviews homeowner-filed complaints in `reports`.
   * Applies strikes: increments `workers.flag_count`. If `flag_count >= 3`, toggles
     `is_suspended = true` to automatically revoke access.
3. Monitoring Status: Queries the `admin_worker_status` View to check days remaining for active
   subscriptions/trials.

--------------------------------------------------------------------------------
3. Database Transaction & Automation Logic
--------------------------------------------------------------------------------

Double-Claim Prevention (RPC)
-----------------------------
An atomic PostgreSQL function `claim_job_securely` ensures that if two workers claim
a job at the exact same millisecond, only the first transaction succeeds.

Signature:
  claim_job_securely(p_job_id UUID, p_worker_id UUID) RETURNS BOOLEAN

Stale Job Release (pg_cron)
---------------------------
To prevent workers from hoarding or ghosting claimed jobs, a background process
runs every 10 minutes to reset jobs to `open` if they remain in `claimed` state for > 90 minutes.

Job Status Reset Conditions:
  * status = 'claimed'
  * claimed_at < now() - INTERVAL '90 minutes'

--------------------------------------------------------------------------------
4. Entity Relationship Summary (Tables & View)
--------------------------------------------------------------------------------

Tables:
  * home_owners:
    - id (UUID, PK, FK to auth.users)
    - full_name (TEXT)
    - phone_number (TEXT, UNIQUE)
    - created_at (TIMESTAMPTZ)
    
  * workers:
    - id (UUID, PK, FK to auth.users)
    - full_name (TEXT)
    - phone_number (TEXT, UNIQUE)
    - fayda_number (TEXT, UNIQUE)
    - fayda_verified (BOOLEAN)
    - trial_ends_at (TIMESTAMPTZ)
    - subscription_expires_at (TIMESTAMPTZ)
    - flag_count (INTEGER)
    - is_suspended (BOOLEAN)
    - created_at (TIMESTAMPTZ)
    
  * admins:
    - id (UUID, PK, FK to auth.users)
    - full_name (TEXT)
    - email (TEXT, UNIQUE)
    - created_at (TIMESTAMPTZ)
    
  * jobs:
    - id (UUID, PK)
    - home_owner_id (UUID, FK to home_owners)
    - worker_id (UUID, FK to workers)
    - title (TEXT)
    - category (TEXT)
    - description (TEXT)
    - photos (TEXT[])
    - offered_price (NUMERIC)
    - status (job_status enum)
    - location_lat (NUMERIC)
    - location_lng (NUMERIC)
    - created_at (TIMESTAMPTZ)
    - claimed_at (TIMESTAMPTZ)
    - started_at (TIMESTAMPTZ)
    - completed_at (TIMESTAMPTZ)
    
  * reports:
    - id (UUID, PK)
    - home_owner_id (UUID, FK to home_owners)
    - worker_id (UUID, FK to workers)
    - job_id (UUID, FK to jobs)
    - reason (TEXT)
    - reviewed_by_admin (UUID, FK to admins)
    - resolution_notes (TEXT)
    - strike_applied (BOOLEAN)
    - created_at (TIMESTAMPTZ)
    
  * subscriptions:
    - id (UUID, PK)
    - worker_id (UUID, FK to workers)
    - amount (NUMERIC)
    - chapa_tx_ref (TEXT, UNIQUE)
    - status (subscription_status enum)
    - chapa_response (JSONB)
    - created_at (TIMESTAMPTZ)

Views:
  * admin_worker_status:
    Queries status of workers and computes remaining days (days_left) as the greatest of 
    remaining trial days and remaining subscription days.